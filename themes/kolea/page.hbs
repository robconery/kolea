{{!< default}}
{{#post}}
<article class="{{post_class}}">
    <header class="post-head">
        <h1>{{title}}</h1>
    </header>
    <div class="prose gh-content">
        {{content}}
    </div>
</article>
{{/post}}
